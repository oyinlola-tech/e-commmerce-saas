<%
  const state = Object.assign({
    statusCode: 404,
    title: 'Page not found',
    message: 'The page or resource you requested could not be found.'
  }, errorState || {});
%>
<%- include('./_error-page', { errorState: state }) %>
