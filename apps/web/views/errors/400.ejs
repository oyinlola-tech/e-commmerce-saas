<%
  const state = Object.assign({
    statusCode: 400,
    title: 'Bad request',
    message: 'The request could not be understood. Check the page input and try again.'
  }, errorState || {});
%>
<%- include('./_error-page', { errorState: state }) %>
