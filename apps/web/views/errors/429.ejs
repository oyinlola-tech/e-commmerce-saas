<%
  const state = Object.assign({
    statusCode: 429,
    title: 'Too many requests',
    message: 'This session has hit a temporary request limit. Please wait a moment and try again.'
  }, errorState || {});
%>
<%- include('./_error-page', { errorState: state }) %>
